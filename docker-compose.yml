services:
  barcodeapi:
    build: .
    container_name: barcodeapi
    ports:
      - "8116:8080"
    volumes:
      - /root/barcodeapi/cache:/cache
      - /root/barcodeapi/config:/config
    restart: unless-stopped
